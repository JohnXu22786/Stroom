/// Stub implementation for web.
///
/// `dart:ffi` is not available on web, so this is a no-op.
/// The real initialization happens in [video_player_init_io.dart].
void registerVideoPlayer() {
  // No operation needed on web
}
